<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <!-- SPORE's brand is the wordmark only — no icon, no mascot. This is a plain
         HARDBRUT accent swatch (yellow field, black border, zero radius), not a
         picture of anything: Android still requires an icon file to exist. -->
    <path
        android:fillColor="#000000"
        android:pathData="M2,2h20v20h-20z" />
    <path
        android:fillColor="#ffd23f"
        android:pathData="M4,4h16v16h-16z" />
</vector>
